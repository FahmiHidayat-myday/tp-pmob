<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tugas_Proyek_Pmob">


        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDMlapKbDd97Ryh7wshr6WppjFeabp__cA" />

        <!-- ================= AWAL PERBAIKAN ================= -->

        <!-- 1. Deklarasi MainActivity sebagai LAUNCHER (pintu masuk aplikasi) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Deklarasikan semua Activity lain yang Anda miliki -->
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ProfileMenuActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DetailLowonganActivity"
            android:exported="false"
            android:label="Detail Lowongan" />
        <activity
            android:name=".activities.DaftarPekerjaanActivity"
            android:exported="false"
            android:label="Daftar Pekerjaan" />
        <activity
            android:name=".activities.LowonganActivity"
            android:exported="false"
            android:label="Lowongan"/>
        <!-- AndroidManifest.xml -->
        <activity
            android:name=".activities.NotifikasiActivity"
            android:exported="false" />


    </application>

</manifest>
